Ollama Modelfile Reference
==========================

My Collection of ollama model configuration file for importing GGUF.

## Intro
Embed `TEMPLATE` and `PARAMETER` from official models library into modelfile with correction.

## Usage
1. Replace the `FROM` section with your gguf path.
2. Copy what you need from the modelfile.
